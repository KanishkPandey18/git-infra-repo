apiVersion: v1
kind: Service
metadata:
  name: alumni-frontend-service
  labels:
    app: alumni-frontend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: alumni-frontend