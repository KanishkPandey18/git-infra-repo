apiVersion: v1
kind: Service
metadata:
  name: alumni-backend-service
  labels:
    app: alumni-backend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http-api
  selector:
    app: alumni-backend