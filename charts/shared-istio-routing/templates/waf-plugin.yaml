{{- if eq (printf "%v" .Values.wafEnabled) "true" }}
apiVersion: extensions.istio.io/v1alpha1
kind: WasmPlugin
metadata:
  name: coraza-waf-{{ .Release.Namespace }} # Differentiates dev vs prod plugin instances inside istio-system
  namespace: istio-system # FORCE it into the gateway's namespace
  labels:
    app: coraza-waf
spec:
  selector:
    matchLabels:
      istio: ingressgateway # Perfectly targets your gateway pod
  url: oci://ghcr.io/corazawaf/coraza-proxy-wasm:latest
  imagePullPolicy: IfNotPresent
  phase: AUTHN
  pluginConfig:
    directives_map:
      default:
        - "SecDebugLogLevel 0" 
        - "SecRuleEngine On"
        - "SecRequestBodyAccess On" 
        - "SecResponseBodyAccess Off" 
        - "Include @coraza.conf-recommended"
        - "Include @owasp_crs/*.conf"
    default_directives: "default"
{{- end }}