apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-email-worker
  labels:
    app: notification-email-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-email-worker
  template:
    metadata:
      labels:
        app: notification-email-worker
    spec:
      containers:
        - name: email-worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["node", "workers/emailWorker.js"]
          env:
            - name: KAFKA_BROKER
              value: {{ .Values.env.KAFKA_BROKER | quote }}
            - name: RESEND_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.secretName }}
                  key: RESEND_API_KEY
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi