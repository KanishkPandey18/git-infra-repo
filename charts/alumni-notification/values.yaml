image:
  repository: 192.168.41.90:8082/alumni-notification
  pullPolicy: Always
  tag: ""

service:
  port: 3000
  targetPort: 3000

env:
  # --- Shared Configurations ---
  secretName: "alumni-secrets"

  # --- Placeholders for ArgoCD Overrides ---
  KAFKA_BROKER: ""