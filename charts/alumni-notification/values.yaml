image:
  repository: avitheg/alumni-notification
  pullPolicy: Always
  tag: "v2"

service:
  port: 3000
  targetPort: 3000

env:
  # kafkaBrokers: "alumni-kafka:9092"
  KAFKA_BROKER: "kafka.{{ .Release.Namespace }}.svc.cluster.local:9092"
  secretName: "alumni-secrets"